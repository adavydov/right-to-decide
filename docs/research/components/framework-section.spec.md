# FrameworkSection specification

## Overview
- Target: `FrameworkSection.tsx`, `FrameworkSection.module.css`
- Interaction: static

## Content
Heading: `Право на решение возникает после пяти разных событий`.
Chain from `rightToDecideChain`; gates from `nonCompensatoryGates`; metrics from `systemMetrics`.
Formula quote: `ИИ удешевляет ответ и удорожает доверие.`
Authority note: university proves educational readiness; employer/regulator grants operational authority.

## Exact styles
- Whole section uses `#292927` background and `#fbfaf7` foreground; section borders accent.
- Intro kicker `#e96a50`; heading white.
- Chain five-column grid margin-top 52px; cells border `rgba(251,250,247,.25)`, padding 24px; numbers accent.
- Gate formula: paper card on dark, margin 42px 0, padding 30px; terms separated by `∧`, Arial 18px/1.5 weight 700.
- Metrics three columns with top accent rule; English metric title Arial 16px; explanation Georgia 16px.

## Responsive
- ≤900px chain 2 columns with last spanning.
- ≤760px all one column, formula terms stack vertically, no overflow.
